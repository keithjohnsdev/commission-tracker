<%= form_with(model: agency) do |form| %>
  <% if agency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(agency.errors.count, "error") %> prohibited this agency from being saved:</h2>

      <ul>
        <% agency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :iata_number, style: "display: block" %>
    <%= form.text_field :iata_number %>
  </div>

  <div>
    <%= form.label :default_commission_rate, style: "display: block" %>
    <%= form.text_field :default_commission_rate %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
